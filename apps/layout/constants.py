# CSS Grid dashboard defaults
GRID_MIN_COL_PX = 320  # default minimum column width for auto-fit grids
GRID_GAP_PX = 16       # default gap in pixels
GRID_MAX_COL_SPAN = 4  # allowed max column span per item
GRID_MAX_ROW_SPAN = 4  # allowed max row span per item

# Backwards-compat shim markers (no longer used after grid refactor)
ALLOWED_COLS = ()
RESPONSIVE_COL_FIELDS = ()
